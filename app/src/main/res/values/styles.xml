<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorSecondary</item>
        <item name="editTextColor">@color/textColor</item>
        <item name="android:colorBackground">@color/background</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="FullyTranslucent" parent="android:Theme.Translucent.NoTitleBar.Fullscreen">
        <item name="android:progressBarStyle">@style/InvisibleProgress</item>
    </style>

    <style name="InvisibleProgress">
        <item name="android:visibility">gone</item>
    </style>

    <style name="NoStatusBar" parent="AppTheme.NoActionBar">
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="ButtonAddMissing" parent="Base.Widget.AppCompat.ImageButton">
        <item name="backgroundTint">@color/btnAddMissing</item>
    </style>

    <style name="ButtonAddDouble" parent="Base.Widget.AppCompat.ImageButton">
        <item name="backgroundTint">@color/btnAddDouble</item>
    </style>

    <style name="ButtonShowMissingOwner" parent="Base.Widget.AppCompat.ImageButton">
        <item name="backgroundTint">@color/btnShowMissingOwner</item>
    </style>

    <style name="ButtonSaveNote" parent="Base.Widget.AppCompat.ImageButton">
        <item name="backgroundTint">@color/btnEditNotes</item>
    </style>

    <style name="AppTheme.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.Launcher" parent="NoStatusBar">
        <item name="android:windowTranslucentStatus">true</item>
    <item name="android:windowBackground">@drawable/splash_screen</item>
    <!-- Optional, on Android 5+ you can modify the colorPrimaryDark color to match the windowBackground color for further branding-->
    <!-- <item name="colorPrimaryDark">@android:color/white</item> -->
    </style>
    
    <style name="UserFormEditText" parent="Widget.AppCompat.EditText">
        <item name="android:drawableTint">@color/signUpIcon</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:ems">10</item>
        <item name="android:backgroundTint">@color/textColor</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="android:textColorHint">@color/hintTextColor</item>
    </style>
    
    <style name="RoundedButton" parent="@style/Widget.AppCompat.Button.Borderless">
        <item name="android:background">@drawable/rec_gradient</item>
        <item name="android:elevation">0dp</item>
        <item name="android:textSize">18sp</item>
        <item name="textAllCaps">false</item>
        <item name="paddingStart">20dp</item>
        <item name="paddingEnd">20dp</item>
    </style>
</resources>
